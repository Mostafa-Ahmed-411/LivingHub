# Agent Rules

- Always ask the user for explicit approval before proposing or executing command-line operations (such as `npm run dev`, `npm run build`, `npm run seed`, etc.). Do not run them automatically or repeatedly on every file edit.
